---
- name: Configure gateway cluster
  hosts: br-cluster
  tags: [ "backup" ]
  gather_facts: true
  become: true

  roles:
    - name: Backup configuration role
      role: cluster/backup
      tags: [ "backup" ]
